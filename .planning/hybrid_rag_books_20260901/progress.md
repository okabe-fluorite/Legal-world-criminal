# 进度

## 2026-09-01
- 完成只读盘点。
- 未修改业务代码、未创建索引、未调用SiliconFlow。
- 独立计划文档和规划目录已创建，等待用户确认。
- 进入阶段2并完成构建器/Schema/父子案例分段/答案隔离首版；合成专项3/3通过。
- 首次真实离线构建处理2,024源、54,463法律/案例子块、1,591案例父段、1,404教材块、1,184 public/private题；唯一失败为1个重复chunk_id，已定位为同文档重复第二条并修正ID设计。
- 修复后真实重建16项门禁全部通过；2,024个源全部匹配既有治理库存，七类Schema全量验证成功，输出hash已写入`HYBRID_RAG_CORPUS_V1_AUDIT`。
- 教师/认知旧smoke兼容已单独提交`c2068cd`；RAG阶段2准备独立提交。
- RAG阶段2已提交`2fc1d6e`；首次push遇GitHub瞬态SSL失败，本地提交安全保留。
- 按用户反馈开始清理普通界面的SHA/内部ID/审计术语，并将构建检查拆为硬阻断与非阻断质量提醒。
- 清理后前端build、可信RAG、技术说明、行末来源抽屉均通过且用户界面无SHA/内部ID；认知smoke只剩数字人旧文案断言待同步。
- 最终回归完成：前端build、可信RAG、技术说明桌面/窄屏、行末引用+实时语音、认知驾驶舱全部通过；技术字段泄漏0、私有字段0、四类浏览器错误0；后端全量131/131通过。
- Hybrid构建检查已分为必须完成检查和非阻断质量提醒；真实全库构建必须项全通过、质量提醒0，Embedding/Reranker调用仍为0。
- 公开阶段12来源抽屉与技术说明截图已更新为无SHA/内部ID版本；完整机器hash仍只留在README/JSON技术报告。
- 清理后前端build、可信RAG、技术说明、行末引用+实时语音均通过；认知smoke唯一失败为仍期望被移除的hash行，已同步为状态/范围2行。

## 2026-09-02
- 阶段3候选模板已生成：120条检索qrels、180对NLI，Gold标签为0，均明确待教师复核。
- 新增两份Schema、模板构建器和专项测试；本地启动配置可从同一SiliconFlow段分别读取Embedding与Reranker模型且不覆盖显式环境变量。
- `test_local_start`与`test_hybrid_rag_eval_templates`共7项通过；下一步进入真实API最小探针，再扩到200—500块分层探针。
- 按最新产品原则，SHA与内部门禁仅保留在开发证据中，普通用户界面不展示；非关键质量提醒不得阻塞整体任务。
- SiliconFlow首次6段/3组最小探针未成功：配置`.ai`路径失败后全球`.com`返回401；未泄漏或改写密钥。根据用户提供的国内官方文档，下一次改为优先尝试`.cn`。
- 调整为国内`.cn`优先后最小探针成功；随后完成300块分层Embedding和30组Rerank真实探针，49次请求全部成功，公开脱敏结果写入`docs/HYBRID_RAG_SILICONFLOW_PROBE.*`。
- 阶段4完成，进入阶段5三库向量索引；用户界面仍不展示内部hash、trace或机器状态码。
- 三个向量集合首轮均完成：法律/案例54,463块、教材1,404块、公开题1,184道。验收发现分批汇总覆盖和题目未使用选项两个实现细节；已修复代码，旧题目索引改为留档后只重建小集合，不重跑主法源向量。
- 阶段5验收完成：57,051条向量、metadata和词法索引逐库对齐，私有答案索引禁用；公开报告写入`docs/HYBRID_RAG_INDEX_V1_REPORT.*`。
- 初版Hybrid运行时与三条真实查询通过；案例父段回填、Dense/Reranker两级降级和精确条号保护已有专项测试，下一步接入产品KnowledgeService并补消融评测。
- 阶段6完成：KnowledgeService已启用治理回投，新增公开分库Hybrid接口；真实HTTP检索、EvidencePack Schema、父段回填和用户界面字段清理均通过。
- 明确不存在法名/条号现在直接友好提示核对，不返回语义相近伪答案；进入阶段7 R0—R4候选消融和材料更新。
- R0—R4真实候选消融完成，R4在候选集上优于BM25F但教材检索仍为0.45短板；公开报告已写入`docs/HYBRID_RAG_ABLATION_V1.*`，全部保持非Gold边界。
- 消融复核发现法源/教材Dense未使用已准备的标题/条号/章节`embed_text`；开始重建这两个索引，首次消融作为content-only基线，重建后再更新最终报告。
- NLI模型初筛首次12批均因OpenCode CreditsError 401失败、预测0；未把失败当结果。按用户授权，仅对明确额度耗尽错误启用DeepSeek官方回退，普通401无效Key仍禁止静默回退。
- 火山引擎主路由写入本地`.env`并验证：3对smoke和180对完整NLI初筛均成功；模型标签三类各60，与自动候选一致率1.0、最低置信度0.90、失败批次0。仍只作教师复核排序，不作Gold。
- embed_text索引重建1702批已完成1700批，2批瞬态失败；断点状态完整，下一次仅重试2批。
- 2个失败批次断点续跑成功，法源/教材embed_text索引均ready；最终R0—R4消融完成并更新公开报告。
- 180对NLI模型初筛完成并生成脱敏公开报告；逐对预测仅留在Git忽略目录，教师Gold仍为0。
- 技术说明Hybrid实验带完成；后端147项、前端类型检查/生产构建、1500×980与780×900浏览器smoke全部通过，用户界面无SHA/内部状态泄漏。
- 启动终端的主路由摘要改为优先显示火山引擎显式主网关；业务路由本身此前已正确使用火山引擎。
- Guizang技术主线V3已完成：新增Hybrid RAG消融页，13页Swiss校验/浏览器截图/PowerPoint实开与回渲染均通过，PPTX低于100MB；完成一次问题发现—修复—复验循环。
- 更新README、Model Adapter和离线演示说明：删除面向队友的外部配置同步命令，改为本地`.env`直接启动，并同步火山引擎与Hybrid RAG真实状态。
- 完成视频V3素材真实性清单、18.6秒精确节拍、旁白/字幕和QA计划；同步录制脚本的Hybrid可见检查与旁白脚本，但遵照`brand-promo-video-generator`在生成前暂停等待用户确认。
- 重建效果报告DRAFT：5页公开报告+2页私密批准表，16项证据源；按PDF技能完成问题发现、术语修复和二次渲染，公开正文不再展示SHA或机器状态词。
- 更新公开包构建器：纳入V3 PPT、效果报告和Hybrid三份公开说明，移除README中的commit/机器状态文案；非核心旧视频接触表不再阻塞DRAFT构建。
- 按用户批注重做PPT第4页数据治理：从混合账单改为“找得到/能引用”两条线，待重新渲染PPTX与公开包。
- 第4页双轨版本完成浏览器与PowerPoint复验；PPT已提交。效果报告和公开提交DRAFT包按该版本重建，源码包排除内部`.planning`并确认包含Hybrid运行时代码。
