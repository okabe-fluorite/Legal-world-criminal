

# 核心 AI 框架 
camel-ai>=0.2.90,<0.3
# CAMEL 0.2.x imports FastMCP from the 1.x MCP package.  MCP 2.x moved the
# symbol and breaks clean installations before the backend can start.
mcp>=1.20,<2
openai>=1.0.0

# 文书生成
docxtpl>=0.16.7
python-docx>=1.1.0
jinja2>=3.1.2
reportlab>=4.2.0

# Web 框架与 API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
websockets>=11.0
python-multipart>=0.0.6
pydantic>=2.0.0

# 系统工具
python-dotenv>=1.0.1
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0
requests>=2.31.0
pymupdf>=1.24.0  # 法条 PDF → legal_corpus/processed JSONL（教学法条库构建）
tzdata>=2025.2  # Windows zoneinfo support for Asia/Shanghai

# 数据库与认证
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0
PyJWT>=2.8.0
pwdlib[argon2]>=0.2.0
